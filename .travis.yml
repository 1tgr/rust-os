addons:
    apt:
        packages:
            - genisoimage
            - libfdt-dev
            - libglib2.0-dev
            - libpixman-1-dev
            - python3-pip
            - zlib1g-dev

before_install: ./travis-setup.sh

cache:
    directories:
        - 3rdparty

dist: trusty

git:
    submodules: false

language: c
script: ./travis.sh
sudo: false

