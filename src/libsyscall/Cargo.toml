[package]
authors = ["Tim Robinson <tim.g.robinson@gmail.com>"]
name = "syscall"
version = "0.0.0"

[lib]
name = "syscall"
path = "lib.rs"
test = false

[dependencies]
core = { path = "../../rust/src/libcore" }
libc = { path = "../liblibc" }

[features]
kernel = []
